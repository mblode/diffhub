---
"diffhub": patch
---

Only render deferred large-file diffs on explicit navigation (active file, prev/neighbor, expanded collapse) instead of any visibility-driven `active` change. Reserve approximate placeholder height per file so scroll position stays stable as patches mount.
